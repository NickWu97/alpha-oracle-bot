name: Alpha-Oracle-Final-Official

on:
  schedule:
    - cron: '*/15 * * * *' # 每 15 分鐘執行一次
  workflow_dispatch: # 支援手動觸發測試

jobs:
  trade_job:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 賦予寫入權限以更新 CSV 紀錄

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4 # 升級至 v4 (支援 Node 24)

      - name: Set up Python
        uses: actions/setup-python@v5 # 升級至 v5 (支援 Node 24)
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: pip install requests pandas numpy

      - name: Run Alpha Oracle Bot
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          # 提前採用 Node 24 運行環境
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true 
        run: python main.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name "Alpha-Oracle-Bot"
          git config --global user.email "bot@alpha-oracle.com"
          git add active_trades.csv trade_history.csv || true
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Auto-update trade logs [skip ci]"
            git push
          fi
